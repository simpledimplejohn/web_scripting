# Node JS with Selenium Wrapper
Scripting with Node js

## About
This uses Node js with a selenium wrapper

## Setup
- `npm install selenium-webdriver`
- `npm -y init`
- install geco driver to 
    `geckodriver /usr/local/bin`
